[project]
name = "sancho-fetch-mcpb-runtime"
version = "0.3.0"
description = "Managed uv launcher for the Sancho Fetch Claude Desktop extension"
requires-python = ">=3.11"
dependencies = [
  "sancho-fetch==0.3.0",
]

